# SIEMA26 — the real build (4 Sep 2026, EOD)

> Yaprak ran the wizard against the MillionVerifier-verified list. Two
> draft campaigns landed on production expo 9 at **15:26:19 UTC**. Monday
> is activation only — no other prep required.
>
> Companion docs:
> - `docs/sessions/SIEMA26_LAUNCH_RUNBOOK.md` — the operational plan
>   (state now BUILT; Monday = activate)
> - `docs/campaigns/SIEMA26/` — the 8 approved templates dumped from DB
> - `docs/sessions/DEPLOY_STEP1_NOT_REGISTERED_20260904.md` — the two
>   code deploys that landed today making step-1 `not_registered` work
>   end-to-end (the wizard used them for the first time here)

---

## 1. Campaigns

Verified read-only 4 Sep after the build:

```
SELECT id, name, status, LEFT(created_at::text,19) AS created
FROM email_campaigns WHERE expo_id = 9 AND created_at >= '2026-09-04';

 id |                           name                           |  status   |       created
----+----------------------------------------------------------+-----------+---------------------
 77 | Morocco Siema Expo 2026 Activate Wave                    | completed | 2026-09-04 07:47:21   -- 2-recipient test
 78 | Morocco Siema Expo 2026 Leena Registered – Re-activation | draft     | 2026-09-04 15:26:19   -- REAL activate wave
 79 | Morocco Siema Expo 2026 Past Visitors – Registration     | draft     | 2026-09-04 15:26:19   -- REAL register wave
```

Both drafts. Nothing sends until Suer activates them.

## 2. Recipient counts

```
SELECT campaign_id, status, COUNT(*)::int AS n
FROM campaign_recipients WHERE campaign_id IN (78, 79)
GROUP BY campaign_id, status ORDER BY campaign_id, status;

 campaign_id | status  |   n
-------------+---------+-------
          78 | active  | 16472
          78 | holdout |   867     (5.00% holdout on the activate wave)
          79 | active  | 16350
          79 | holdout |   861     (5.00% holdout on the register wave)
```

**Totals: 32,822 active + 1,728 holdout = 34,550 recipients.** Confirmed the 5% holdout Suer set in the Confirm panel: 867 / (16472+867) = 5.00%; 861 / (16350+861) = 5.00%.

## 3. Drip topology

```
SELECT campaign_id, step_number, template_id, delay_hours, condition
FROM campaign_steps WHERE campaign_id IN (78, 79) ORDER BY campaign_id, step_number;

 campaign_id | step_number | template_id | delay_hours |   condition
-------------+-------------+-------------+-------------+----------------
          78 |           1 |          74 |           0 | not_registered
          78 |           2 |          75 |          72 | not_registered
          78 |           3 |          76 |          96 | not_registered
          78 |           4 |          77 |          72 | not_registered
          78 |           5 |          78 |          96 | not_registered
          79 |           1 |          79 |           0 | not_registered
          79 |           2 |          80 |         240 | not_registered
          79 |           3 |          81 |          96 | not_registered
```

**All 8 steps `not_registered`.** Step 1 = 0h (fires on activation, wizard's `normaliseStep1` at `campaignBuilder.js:869` forced this). The `not_registered` on step 1 was made honourable end-to-end by `87c6c4c` (worker guard-move) and `3120810` (campaigns.js activation gate).

Templates map exactly to `docs/campaigns/SIEMA26/`:
- Activate: `01_invitation` (74) · `02_univers_exposants` (75) · `03_programme_conferences` (76) · `04_plan_de_visite` (77) · `05_dernier_appel` (78)
- Register: `R1_invitation` (79) · `R2_plan_de_visite` (80) · `R3_dernier_appel` (81)

## 4. Reactivation tokens

```
SELECT COUNT(*)::int AS total_tokens,
       COUNT(*) FILTER (WHERE form_id = 59)::int AS with_form_59,
       COUNT(*) FILTER (WHERE form_id IS NULL)::int AS null_form_id,
       COUNT(*) FILTER (WHERE phone ~ '^\+')::int AS e164_phones,
       COUNT(*) FILTER (WHERE COALESCE(phone,'') = '')::int AS empty_phones,
       COUNT(*) FILTER (WHERE phone IS NOT NULL AND phone !~ '^\+' AND phone <> '')::int AS non_e164
FROM reactivation_tokens
WHERE target_expo_id = 9 AND created_at >= '2026-09-04 00:00:00+00';

 total_tokens | with_form_59 | null_form_id | e164_phones | empty_phones | non_e164
--------------+--------------+--------------+-------------+--------------+----------
        16471 |        16471 |            0 |       15735 |          736 |        0
```

- **All 16,471 tokens carry `form_id = 59`** (the SIEMA French visitor form) — proves the wizard's Phase 2b propagation from 3 Sep is doing its job; every activation click lands on `reactivate-fr.html` rendered with form 59's branding.
- **15,735 phones (95.5%) in E.164** — proves `resolveCountry` + `normalizePhone` (Decision B, 2 Sep) ran cleanly. 736 empty phones are legitimate source rows without a phone.
- **Zero non-E.164 phones** — no partial-normalisation artefacts.
- **Zero tokens with NULL form_id** — no reused-with-old-metadata rows survived Phase 2b.

Delta 16,472 - 16,471 = 1 recipient on campaign 78 whose token wasn't minted this run (an existing active token from campaign 60's test on the same email — reused per Phase 2b).

## 5. Recipients who registered between build and now

Suer's build-time build report named **8 recipients** who had already registered on expo 9 between the wizard building the recipient list and the campaign row landing in the DB. Measured EOD (~5 h post-build):

```
WITH c AS (SELECT id, created_at FROM email_campaigns WHERE id = 78)
SELECT COUNT(DISTINCT lower(cr.email))::int AS cnt
FROM campaign_recipients cr, c
WHERE cr.campaign_id IN (78, 79)
  AND cr.status = 'active'
  AND EXISTS (SELECT 1 FROM visitors v
              WHERE v.expo_id = 9 AND lower(v.email) = lower(cr.email)
                AND v.created_at > c.created_at);

 cnt
-----
  11
```

**11 as of EOD, up from 8 at build.** The set will grow further between now and Monday 11:00 IST activation (recent 24-h velocity has been ~500 registrations/day). Every one of them will be silently skipped by the worker at step 1 send-time — the whole reason `not_registered` was pushed to step 1 today (see `DEPLOY_STEP1_NOT_REGISTERED_20260904.md`).

## 6. Template re-audit

Re-ran the wave-aware validator + forbidden-fragment scan on all 8 templates as they stand at build. **All 8 PASS every check** (see `DEPLOY_SIEMA_BATCH_20260903.md` for the audit script and column meanings; the run just before build was identical):

| id | wave | validator | bare-name | forbidden | href/link | chain=1 | subject | preheader | size |
|---|---|---|---|---|---|---|---|---|---|
| 74 | A | PASS | PASS | PASS | PASS(href) | PASS(1) | PASS | PASS(126c) | 22,097 b |
| 75 | A | PASS | PASS | PASS | PASS(href) | PASS(1) | PASS | PASS(113c) | 22,378 b |
| 76 | A | PASS | PASS | PASS | PASS(href) | PASS(1) | PASS | PASS(104c) | 14,997 b |
| 77 | A | PASS | PASS | PASS | PASS(href) | PASS(1) | PASS | PASS(118c) | 14,944 b |
| 78 | A | PASS | PASS | PASS | PASS(href) | PASS(1) | PASS | PASS(89c) | 8,072 b |
| 79 | R | PASS | PASS | PASS | PASS(form59) | PASS(1) | PASS | PASS(112c) | 22,453 b |
| 80 | R | PASS | PASS | PASS | PASS(form59) | PASS(1) | PASS | PASS(129c) | 14,702 b |
| 81 | R | PASS | PASS | PASS | PASS(form59) | PASS(1) | PASS | PASS(88c) | 8,411 b |

Verdict: **READY.** Yaprak's morning fix on template 74 (removed the `<a href="{{unsubscribe_url}}">` anchor per the earlier audit) held.

## 7. The Send-Emails first_name finding — CLOSED for Monday

Yaprak's earlier "Bonjour Güzelcik," greeting on a template-76 test yesterday came from the `email-send.html` bulk path (`routes/emailSend.js`), NOT the campaign worker. Full narrative in the audit reply to Job B (transcript).

**The campaign path renders correctly.** `email_worker.js:614-623` builds `data` with explicit `first_name: recipient.first_name || ''` from `campaign_recipients.first_name` — which the wizard populates from the source row's `name` column at `campaignBuilder.js:1166/1182/1205`. Yesterday's four live-test recipients (`Elif`, `Test`, `Plus`, `Nihat`) all rendered correctly. Monday's 34,550 recipients will too.

The Send-Emails bug is a **test-path-only** issue; smallest fix is ~10 lines in `emailSend.js` `/single` + `/bulk` (map `visitors.name → emailData.first_name`, `visitors.last_name → emailData.last_name`) — **queued in todo.md as P2 for post-fair**. Not on the critical path for SIEMA.

New gotcha `G38` records this so Yaprak (or a future ops user) doesn't send another `email-send.html` blast expecting first-name greeting to work.

## 8. Monday plan — activation only

**Nothing to prep. Activate at the wall-clock times below:**

| Time (Europe/Istanbul) | Time (Africa/Casablanca) | Action |
|---|---|---|
| **Mon 8 Sep 11:00** | 09:00 | Activate **campaign 78** (Leena Registered – Re-activation) — 16,472 active, step 1 = template 74 |
| **Mon 8 Sep 13:00** | 11:00 | Activate **campaign 79** (Past Visitors – Registration) — 16,350 active, step 1 = template 79 |

**Do not touch anything else.** No template edits (see `G7` — templates are referenced live, so an edit while a campaign is active reaches its next step). No wizard re-run (Phase 2b would UPDATE reused tokens' form_id — same value 59, no net change, but pointless work). No email-send.html bulk sends to template-76-style copy (see `G38`).

**Both waves land in the worker in one drain window** (~13:00-13:10 IST for the 16,472 activate step-1s at ~275/min; ~13:00-15:00 IST for the 16,350 register step-1s starting at 13:00). Watch the SendGrid dashboard for bounce/complaint. `email_queue.status='sent'` for live delivery truth (G14).

**Everyone who registered by any route between build (Sat 15:26 UTC) and activation (Mon ~08:00-10:00 UTC) is skipped** — this is the whole reason `not_registered` shipped to step 1 today.

## 9. Deferred (post-fair)

- **`G37` / `G38` / `G39` / `G40`** — gotchas from today's work, see CLAUDE.md.
- `emailSend.js` first_name mapping — P2, ~10 lines, post-fair.
- Thank-you page feature (Job B Option A) shipped on `feat/thankyou-page` branch — Saturday-morning deploy on Suer's go.
- Form 59 still missing `hear_about_event` field — still open for Yaprak (pre-fair decision item from 3 Sep runbook §6).
